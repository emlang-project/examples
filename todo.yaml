# Direct form slices: simple sequences without tests or props
# Multiple slices in a single document
---
slices:
  AddTask:
    - t: User/TaskForm
    - c: AddTask
    - e: TaskAdded
    - v: TaskList

  RemoveTask:
    - t: User/TaskList
    - c: RemoveTask
    - e: TaskRemoved
    - v: TaskList
